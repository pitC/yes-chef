{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "hosting": {
    "public": "public",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "predeploy": [
      "if [ -z \"$SKIP_SW_BUMP\" ]; then node scripts/bump-sw-cache.mjs; else echo \"SKIP_SW_BUMP set — skipping SW cache bump (already bumped by caller)\"; fi"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
